# Google Sheet published to the web as CSV. Read only.
SHEET_CSV_URL=

# Server side only. Never exposed to the browser.
ANTHROPIC_API_KEY=

# Postgres. Vercel Postgres (Neon) in production, local Postgres or a Neon dev branch for npm run dev.
DATABASE_URL=
